{
	"name": "sociotickets",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "vite dev --host",
		"build": "vite build",
		"preview": "vite preview",
		"release": "npm run build && git add . && git commit -m \"another release\" && git push && git subtree push --prefix dist origin gh-pages",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^2.0.0",
		"@sveltejs/kit": "^1.5.0",
		"svelte": "^3.54.0",
		"svelte-check": "^3.0.1",
		"tslib": "^2.4.1",
		"typescript": "^5.0.0",
		"vite": "^4.2.0"
	},
	"type": "module",
	"dependencies": {
		"@sveltejs/adapter-static": "^2.0.1",
		"@vitejs/plugin-basic-ssl": "^1.0.1",
		"html5-qrcode": "^2.3.7",
		"jsqr": "^1.4.0",
		"vite-plugin-mkcert": "^1.13.4"
	}
}
